# Configuração da API (OPCIONAL)
# Se não definido, usa detecção automática baseada no hostname

# Para desenvolvimento local:
# VITE_API_URL=http://localhost:8000

# Para rede local (substitua pelo IP da máquina):
# VITE_API_URL=http://10.160.200.44:8000

# NOTA: Se não definir VITE_API_URL, o sistema detecta automaticamente
# usando o mesmo hostname que o frontend está sendo acessado
